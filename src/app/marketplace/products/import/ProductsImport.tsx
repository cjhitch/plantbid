import { type NextComponentType } from 'next';

const ProductsImport: NextComponentType = () => {
	return (
		<>
			<h1>this is the products import file</h1>
			<h2>sub section</h2>
		</>
	);
};

export default ProductsImport;
