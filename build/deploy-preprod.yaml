app:
    healthCheck:
        path: /api/healthcheck
        protocol: HTTP
        port: 3000
    networking:
        ingress:
            loadBalance: true
            port: 3000
    resources:
        instances: 1
        requests:
            cpu: 0.25
            memory: 128
        limits:
            cpu: 3
            memory: 256
    name: next_template
    domain: 
    role: 
    group: 
    version: 0
    container:
        image: next_template
